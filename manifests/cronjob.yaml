apiVersion: batch/v1
kind: CronJob
metadata:
  name: market-scanner
spec:
  schedule: "0 * * * *"
  jobTemplate:
    spec:
      template:
        spec:
          containers:
          - name: scanner
            image: market-scanner:latest
          restartPolicy: OnFailure
